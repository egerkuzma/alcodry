# Спецификация остаётся локальной, в репозиторий не едет.
SPEC.md

__pycache__/
*.py[cod]
.venv/
.pytest_cache/
data/*.db
data/*.db-wal
data/*.db-shm

# Сборка маковского клиента: .app и .dmg собираются из исходников.
mac/build/
